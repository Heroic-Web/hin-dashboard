services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: hin-app
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=postgresql://hin:hin@db:5432/hin
      - JWT_SECRET=${JWT_SECRET:-change-me-to-a-random-string-min-32-chars}
      - RESEND_API_KEY=${RESEND_API_KEY:-}
      - RESEND_FROM_EMAIL=${RESEND_FROM_EMAIL:-hin <onboarding@resend.dev>}
      - ADMIN_EMAILS=${ADMIN_EMAILS:-}
      - NEXT_PUBLIC_APP_URL=${NEXT_PUBLIC_APP_URL:-http://localhost:3000}
    depends_on:
      db:
        condition: service_healthy
    networks:
      - hin-network

  db:
    image: postgres:16-alpine
    container_name: hin-db
    restart: unless-stopped
    environment:
      - POSTGRES_USER=hin
      - POSTGRES_PASSWORD=hin
      - POSTGRES_DB=hin
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U hin"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - hin-network

volumes:
  postgres_data:

networks:
  hin-network:
    driver: bridge
